# conda env create -f environment.yml
name: uspas-ml
channels:
  - conda-forge
dependencies:
  - python=3.9
  - ipykernel
  - ipywidgets
  - jupyterlab
  - matplotlib 
  - scipy
  - scikit-learn
  - xopt
  - pip
  - pip:
      - cheetah-accelerator
